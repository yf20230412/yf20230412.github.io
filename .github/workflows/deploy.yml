name: Deploy VitePress site to Pages

on:
  push:
    paths:
      - 'docs/docs/**'
  workflow_dispatch:

env:
  TZ: Asia/Shanghai  

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - uses: pnpm/action-setup@v3
        with:
          version: 9
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          
      - name: Install dependencies
        run: pnpm install
      
      - name: Build with VitePress
        run: pnpm run build
      
      - name: Debug build output
        run: |
          echo "Checking if docs/docs/.vitepress/dist/ exists..."
          ls -l docs/docs/.vitepress/dist/
      
      - name: Ensure docs/bk directory exists and has write permissions
        run: |
          mkdir -p docs/bk
          chmod -R 755 docs/bk
      
      - name: Move build output to docs/bk
        run: |
          rm -rf docs/bk/*
          cp -R docs/docs/.vitepress/dist/* docs/bk/
      
      - name: Debug final output
        run: |
          echo "Checking contents of docs/bk..."
          ls -l docs/bk/
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/bk

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4